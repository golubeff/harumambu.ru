    { 
      'id' : <%= project.id %>, 
      'budjet' : '<%=raw project.budjet_with_currency %>', 
      'title' : '<%=raw project.title %>', 
      'desc' : '<%=raw(project.desc) %>',
      'created_at' : '<%=l project.created_at %>',
      'url' : '<%= project.url %>',
      'klass' : '<%= project.klass %>'
    }

